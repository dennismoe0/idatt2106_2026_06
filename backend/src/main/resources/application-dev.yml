# Dev profile — activate with: SPRING_PROFILES_ACTIVE=dev ./mvnw spring-boot:run
# Wipes and re-seeds test data on every startup.
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${MYSQL_PORT:3307}/${DB_NAME:nettdetektivene}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8&useUnicode=true

app:
  dev-seed: true
  bypass-stop-lock: true
